# yaml-language-server: $schema=https://garnix.io/api/garnix-config-schema.json
builds:
  include:
    - "checks.*.honeypot-bot-clippy"
    - "checks.*.honeypot-bot-debug-clippy"
    - "checks.x86_64-linux.treefmt"
  branch: main
